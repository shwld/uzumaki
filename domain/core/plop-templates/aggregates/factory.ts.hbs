import { {{pascalCase name}}Entity, Updatable{{pascalCase name}}EntityFields } from '../../models';
import { generateTimeStampProperties } from '../../shared/entity';

export const build{{pascalCase name}} = (
  {{name}}Params: Updatable{{pascalCase name}}EntityFields & { id: string }
): {{pascalCase name}}Entity => {
  return new {{pascalCase name}}Entity({
    ...generateTimeStampProperties(),
    ...{{name}}Params,
    isDeleted: false,
  });
};
