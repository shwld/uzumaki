import { EntityState } from '../../shared';
import type { {{pascalCase name}}_Attributes } from './{{kebabCase name}}-interfaces';

export type {{pascalCase name}}Entity = {{pascalCase name}}_Attributes & EntityState & {};

export function {{pascalCase name}}Entity(item: {{pascalCase name}}_Attributes): {{pascalCase name}}Entity {
  return {
    ...item,
    __state: 'Entity',
  };
}
