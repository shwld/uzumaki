type UserEdge implements Edge {
  node: User
  cursor: String
}

type UserConnection implements Connection {
  pageInfo: PageInfo
  edges: [UserEdge]
}

type User implements Node {
  id: ID!
  name: String!
}
