type StoryEdge implements Edge {
  node: Story
  cursor: String
}

type StoryConnection implements Connection {
  pageInfo: PageInfo
  edges: [StoryEdge]
}

type Story implements Node {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
}
