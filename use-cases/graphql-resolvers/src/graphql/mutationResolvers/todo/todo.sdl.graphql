input TodoInput {
  id: ID!
  title: String!
}

type TodoSuccessResult {
  result: Todo!
}

union TodoMutationResult =
    TodoSuccessResult
  | RecordInvalidResult
  | UserErrorResult

type Mutation {
  createTodo(input: TodoInput!): TodoMutationResult!
}
