import { QueryResolvers } from '../../../../generated/resolvers-types';

export type {{pascalCase queryName}}QueryResolvers = Pick<QueryResolvers, '{{queryName}}'>;

export const {{queryName}}: {{pascalCase queryName}}QueryResolvers['{{queryName}}'] = (
  _parent,
  args,
  ctx,
  _info
) => {
  return ctx.currentUser;
};
