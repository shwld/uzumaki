import { {{ pascalCase objName }}Validator } from 'core-domain';
import { z } from 'zod';

export const {{ camelCase action }}{{ pascalCase objName }}ArgsValidationSchema = z.object({
  input: z.object({
    id: {{ pascalCase objName }}Validator.validators.id,
  }),
});
