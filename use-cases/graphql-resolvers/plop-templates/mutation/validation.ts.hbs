import { {{camelCase objName}}ValidationSchema } from 'core-domain';
import { z } from 'zod';

export const {{camelCase action}}{{pascalCase objName}}ArgsValidationSchema = z.object({
  input: {{camelCase objName}}ValidationSchema.pick({
    id: true,
  }),
});
